import{test,expect}from'@playwright/test';
test('fresh qualified runtime supplies node logs to approved inspector',async({page})=>{test.skip(!process.env.CLAB_LOG_LIVE,'Explicit fresh log qualification required');await page.goto('/');await page.getByLabel('Approved lab',{exact:true}).selectOption('RUNTIME-PAIR');await page.getByRole('button',{name:'Load native declarations',exact:true}).click();await expect(page.getByRole('button',{name:'Select left',exact:true})).toBeVisible({timeout:15000});await page.getByRole('button',{name:'Select left',exact:true}).click();await page.getByRole('button',{name:'Connect enrolled runtime'}).click();await page.getByRole('button',{name:'Logs',exact:true}).click();await page.getByRole('button',{name:'Load logs',exact:true}).click();await expect(page.getByLabel('Node log output')).toContainText('EXP027_MARKER');await page.getByLabel('Follow logs',{exact:true}).check();await expect(page.getByRole('button',{name:'Stop logs',exact:true})).toBeEnabled();await page.getByRole('button',{name:'Stop logs',exact:true}).click();await expect(page.getByLabel('Follow logs',{exact:true})).not.toBeChecked();await page.screenshot({path:'experiments/EXP-027-node-logs/live-logs.png',fullPage:true});});
