# User-directed scope and executing brief

User: Okay, for this project, we do not need the authorization R2 or R3. This is a test environment, so the authorization is excessive.

Assistant interpretation accepted by “proceed with the next step”: single-user trusted test environment; per-user/per-job authorization out of scope, not passed. Retain native correctness, filesystem/network isolation, limits and no privileged Docker socket in the resolver, metadata allowlisting/compatibility/expiry. Next progress broader native coverage, provenance and source-to-graph projection.

User: proceed with the next step

This changes authorization requirements, not the prohibition on accessing pre-existing VMs, the read-only investigation, source preservation, or the remaining data-handling requirements. Existing authorization evidence stays historical. No public/multi-user service is requested.
