# LIVE-003 — interactive review found and fixed offline GO handling

**Observed:** A35's2697 manifest hashes verified with no drift. Safari showed the lab stopped. The user explicitly approved resuming it. GO against the stopped owned VM returned DEPLOYMENT_FAILED and incorrectly persisted partial deployment. The actual runtime connection failed before native deployment. After the authorized runtime resume, the exact project had no deployment.json and Docker contained no containers.

**Implemented:** read-only owner/native-identity preflight runs under the lifecycle busy guard before deployment intent. A failed preflight leaves ready/stopped unchanged and GO retryable. Original files are rechecked after the asynchronous preflight. Failures after native deployment intent still remain partial and require scoped recovery. The GUI explains runtime preflight failure and possible partial deployment, and tells users to use GO before logs/capture when stopped. No material layout change, new service, runtime helper or topology semantics.

**Observed verification:**22 TypeScript live tests and9 Python helper tests pass; typecheck and fixture-free build pass. New independent tests cover offline ready/stopped preservation, no deployment invocation/persisted intent, retry, simultaneous GO/Stop refusal, and source changes during preflight. Offline failures are mocked; the user runtime was not stopped again to inject failure. Existing native-failure partial behavior remains tested. External preview.7 archive hashes verified and no fixture bundles shipped.

**Scoped recovery:** preserved the erroneous prior host state in a private backup and recorded its hash in offline-recovery.json. Restored only the exact owner/project with null deployment, after proving no native deployment record and empty container inventory. No old container identity was adopted. The old app server alone was restarted with the new archive using the same input file inventory. General automatic recovery/migration and runtime resume UI remain unimplemented.

**Not rerun:** official demo deployment, PCAP/reanalysis, serial, full corpus, long-duration soak, Apple Installer transaction/signing. Their A35 historical evidence is unchanged, not promoted to new qualification. Package remains an unsigned local archive preview; pkgbuild permission diagnostics remain unresolved. No new VM was created; the specifically authorized durable user runtime was resumed, and no qualification VM was touched.

**Next:** further interactive presentation review and explicit runtime resume/recovery UX. The first review was redirected to this observed lifecycle bug rather than speculative cosmetic changes. TT-01,177 denominator, historical Q results and approved layout remain unchanged. B4/B6 lifecycle/user-experience and S-01/S-03/S-07 receive narrowly scoped preflight/disclosure/recovery evidence.

**Observed installed result:** Safari GO completed; all eight native nodes displayed running, processor eth1 observed/admin up/carrier up, real logs loaded (51 fetched lines), Follow refreshed to57 lines with a newer timestamp. The preview.7 server and explicitly resumed user runtime remain running at4173. 85 prior regression tests also passed. Native-kind/application-health conclusions are not inferred from these UI results. See interactive-review.json.
