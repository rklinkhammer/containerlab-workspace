# LIVE-005 — installed runtime resume and scoped recovery

A37 baseline verified. Preserve running user lab; use a separate fresh qualification VM and separate state directory for stop/resume/fault trials. Explicit installed runtime status/start commands must check saved host-instance identity before boot and guest owner/native pins afterward. Existing running runtime may prepare an offline-resume identity only after verified live ownership. Never adopt a stopped unanchored instance or auto-deploy on resume.

Recovery must be read-only for containers: classify absent/partial/exact matching running deployment under the native operation lock. Empty verified inventory can clear obsolete project deployment intent; live replacement IDs refuse. Partial resources require explicit Stop lab; never silently enroll them. Serialize recovery with GO/Stop. Preserve safe reason codes and show actionable diagnostics without native stderr. Independent tests: missing/replaced host anchor refuses start; stopped status works; start is idempotent; post-start identity mismatch refuses; absent recovery resets safely, partial stays partial, replacement refuses; runtime restart preserves identities. Qualification cleanup stops only its new VM. User lab remains running.

User steering: explicitly permits stopping/restarting the running user lab for development. Use only clab-app-3a2d71d640034b2d for these authorized checks; no new VM required for this approved existing-runtime scope. Restore running at completion.
