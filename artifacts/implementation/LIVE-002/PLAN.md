# LIVE-002 — installed live workflow and selected-link capture

Authorization: user requested continuation after freeing 4173; original LIVE-001
executing brief remains applicable. Preserve A34 and its regenerated-evidence drift.

Independent acceptance, before execution:
- Installed app outside repository loads explicitly inventoried user files through native
  APIs. No packaged fixture catalogs or recorded graph substitute.
- GO creates exactly the declared containers, native interface identities are enrolled,
  node log text is real, reconnect does not redeploy, Stop removes only owned resources.
- Selected veth link resolves one exact qualified Linux endpoint automatically, observing
  both directions. Parallel occurrences cannot alias. No endpoint picker in the browser.
- PCAP filename is a basename ending .pcap, not a filesystem path. One in-memory artifact,
  maximum 1MiB, 5-minute TTL, no silent overwrite of a host file. Browser save uses the
  recorded filename; bytes/hash must survive download and reanalysis unchanged.
- Capture duration 1–10 sec; snaplen64–65535; 100 displayed packet rows. Capture filter is
  distinct from display filter. Only the already reviewed Lua profile may execute, in its
  existing minimal sandbox. No arbitrary script/path/command accepted.
- Cancellation, stop and identity changes must prevent publication of stale captures.
  Unsupported native link/kind/profile returns an explicit reason, never a fake target.

Runtime: explicitly invoke installed `runtime create` to create a new unique owned VM;
no pre-existing VM access. This run is qualification, not the final continuous user lab.
Use its generated ownership record, no host mounts, no agent forwarding, loopback4173.
Record exact image/package identities. Clean up task-owned lab and stop this VM after
qualification; remove only the newly-created default pointer after recording ownership,
so later user provisioning cannot accidentally reuse a stopped trial. Existing state
must cause refusal. Budgets: 8CPU/16GiB/50GiB, bounded capture/scratch, finite test duration.

Selected official demos retain LIVE-001 independent expectations and original hashes;
177-case denominator unchanged. Full four-radio and image acquisition are separate
acceptance stages. Never turn synthetic installed success into a full-corpus claim.
