# EXP-034 — Phase5 integrated GUI acceptance

Expectations written before execution: one unmocked browser scenario must load actual RUNTIME-PAIR declarations, connect exact enrollment, observe native state, fetch/follow/stop synthetic node stdout, switch nodes without stale logs, capture task-owned mixed UDP sequence7/8 traffic through the selected Linux endpoint, filter via reviewed Lua, reanalyze the same bytes for sequence8, verify disjoint matching frame numbers plus unchanged SHA/expiry/download bytes. Invalid display filter must retain valid PCAP. Disconnect during active capture must remove the binding and refuse late results; after the independent worker deadline reconnect and complete another capture. Session capability withdrawal must refuse operations; restoration requires explicit reconnect, not reuse of a stale output. Navigation clears prior capture/log output. No GUI deployment, application-health inference or serial transport.

Use actual APIs with no Playwright route mocking in runtime acceptance. Synthetic markers differ by node and contain safe hostile markup to verify literal rendering. Real runtime failures and any corrective attempts stay recorded. Existing unit/replay tests are separate. Scenario results must bind exact fresh VM/session/tool hashes; initial setup must use explicit fresh-session workflow. Ordinary tests skip runtime unless explicitly enabled and never create VMs. Capture <=10sec/1MiB, synthetic traffic finite, max scenario duration3minutes. Remove task-owned lab and stop new VM even on failure. Preserve historical Q scores,177 corpus denominator and TT-01.

Selected Phase5 profile only: Linux RUNTIME-PAIR, reviewed synthetic Lua; no universal corpus/kind coverage, production release, VITA Lua, QEMU or accessibility-conformance claim.
