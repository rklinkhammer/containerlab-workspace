# EXP-024 — consolidation and migration, before implementation

**Designed:** active collection has three duplicated paths: observation-session.py fixed left/right enrollment versus generic plan; inspect.py fixed-pair collector versus multi.py (which imports legacy Reader/namespace); backend associateStates/associateProfile versus associateMulti. Fixed pair functions are retained only as historical test oracles, not application runtime alternatives. Shared behavior: bounded subprocess reader, native before/after inventory, exact full container IDs, namespace/interface attributes, native literal/alias selection, Linux supplemental flags. Intentional variation is native kind policy and declared graph shape, not separate collectors.

Choose observation/0.7 for every newly enrolled profile, enrollment/0.2 and explicit sessionFormat observation-session/0.2. Old0.1–0.6 DTO validators stay strict readers; historical recordings unchanged. Old session markers or plan versions return INCOMPATIBLE_SESSION before spawning limactl, even if expired; no automatic migration. New explicit VM/session enrollment only. Native authority/source/bundle/occurrence hashes and unknown peer/continuity/health remain unchanged.

Independent expectations.json is fixed before execution: existing EXP-023 three profiles verbatim plus explicit left/right expectations for Linux and SRL pairs. All five existing fixture YAML/catalog bytes remain unchanged. Pair aliases come from prior actual/pinned native evidence, never application string conversion. Unavailable SRL alias versus successful Linux absence, unsupported/partial statuses, disconnected state, complete full-ID rejection and global timeout/output semantics stay unchanged.

Fresh task sessions .runtime/exp024-{linux,srl,baseline,medium,max}; no previous VM accessed or reused. One profile per new VM, existing8CPU/16GiB pinned recipe, source and immutable images. Only synthetic approved labs; no traffic generator/public ingress/host mounts/agent forwarding. Destroy exact lab, verify containers/network absent and stop each new VM in cleanup/finally. New EXP-024 output root; no historical writes.

Qualification:20 guest and20 host samples plus10 browser refreshes per profile after separate warmups (same A18 sample counts, extended to pairs). Independent field expectations checked each host sample. Thresholds unchanged: native<6000ms/<=262144 combined bytes, transport<9000ms, DTO<=262144 bytes, browser<12000ms with nearest-rank p95<=5000ms, heartbeat gap<=250ms; command count2+2*connected nodes. Bounded local samples, not SLO/soak/general topology guarantees.

Run all five fresh enrollments/browser regressions; max injected process failures and native transitions; pair actual transitions on their own sessions after healthy checks. Test incompatible manifests with a runtime-call sentinel (offline), historical DTO parsing/malformed rejection, unsupported kinds/roles, partial enrollment, max/over-limit, malicious text, stale/cancel/superseded behavior. Label injected processes, mocked outputs and replay distinctly. No changed acceptance semantics merely to match code; only explicit version/session changes warrant version assertions changing.

Limits, TT-01, historical Q gates and177 denominator remain. D-15 verified A18 archive plus executing brief/replaced external docs before publication. Rollback: disable live session, restore A18 Git source/design archive; do not reuse stopped VM. Fresh A18 setup would be required. Existing recorded-only previews stay usable without compatible live session.
