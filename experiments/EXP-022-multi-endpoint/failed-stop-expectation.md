**Observed:** first V2 transition attempt passed initial four occurrences, independent state, alias loss, actual duplicate aliases, deletion and replacement guards. It failed at one-node-stopped: the test expected client eth2 still observed. Stopping the router namespace also removes its veth peer; native complete client inventory reports absence. This is valid client evidence, not inspection failure. The corrected assertion requires MISSING_FROM_NATIVE_INVENTORY, client running and isolated running; it does not fabricate an observed interface or weaken association policy. Original runtime-multi-v2.log and attempt results remain. Re-run entire transition suite on separately created exp022-multi-runtime VM, same unchanged V2 fixture.
