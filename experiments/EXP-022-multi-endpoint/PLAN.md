# EXP-022 — bounded occurrence enrollment

**Designed (before execution):** qualify our projection/enrollment and GUI, not Containerlab generally. Accepted native declaration graphs alone supply node IDs and ordered link/endpoint occurrence IDs (`linkId:endpoint:position`). No YAML parsing or alias calculation. Approved MULTI-ENDPOINT adds router (SRL), host and isolated (Linux), two veth links and four occurrences. Independent expectations are in expectations.json; original bytes and catalog hashes are preserved.

Limits: 8 nodes, 16 links, 32 endpoint occurrences, 64 native interfaces per node; aggregate native subprocess output 262144 bytes and 6 seconds, host transport 9 seconds, browser 12 seconds. One native interface inventory and one Linux link inventory per supported node with endpoints; disconnected nodes need container inspection only. Collection concurrency one; refresh minimum 1 second; optional polling 5 seconds; freshness 15 seconds. No budget increase. Maximum-size validation is not a capacity/performance qualification.

Runtime policy: only new task VMs, explicit session directories under .runtime/exp022-*, approved synthetic bundles only. No previous VM queried or reused. Native declaration worker gets no socket. Collector target plan installed root-owned, derived from validated graph; browser cannot supply it. Full container IDs fixed at enrollment; replacement rejects collection. Missing enrollment stays explicit; unsupported kinds/roles stay declared. Legacy pair profiles retain their existing DTOs; the new profile uses observation/0.5 and fresh enrollment only.

Independent checks: initial four associations, parallel links remain distinct, isolated node has state and zero endpoints; first interface down leaves second unchanged; alias loss is unavailable, Linux deletion is absent only with complete inventory; duplicate aliases unavailable (mock if native tool cannot produce ambiguity); partial per-node inspection preserves others; full ID replacement rejected; namespace changes unresolved; cancellation, stale/superseded responses, output/time caps and oversize schemas fail closed. Mock faults are labeled separately. Existing pair profiles need fresh runtime/browser regression trials.

Cleanup: destroy fixed task lab, check native inventory empty and task management network absent, stop exact new VM even after failures. Preserve failed attempts. No capture, terminals, new kinds, peer/continuity/forwarding claims. Historical Q results and 177 denominator unchanged.

Pre-V2 execution correction: original host node name is a native reserved endpoint role. Preserve original fixture/hash, multi-session.json, browser-multi.log and expectations-original-host.json. MULTI-ENDPOINT-V2 uses client; expectations.json records the same intended topology under a nonreserved node name. New task session exp022-multi-v2, no reuse of first deployment.
