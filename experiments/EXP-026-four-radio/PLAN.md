# EXP-026 four-radio acceptance

Question: can the unchanged user-owned four-radio bundle be loaded, rendered and explicitly associated with its actual deployment without confusing network facts with SDR health?

Independent expectations.json was written from reviewed topology bytes before runtime execution:8 nodes,7 exact switch-to-application links,14 endpoint occurrences, SR Linux ethernet-1/1..7 aliases. Source provenance hashes bind all generated companions, build inputs and actual VRT submodule. The initial preparation command used a relative sibling path incorrectly and failed before admission; corrected to resolved root (no source changed).

Dedicated fresh VM only. First load declarations using the unchanged isolated socket/network-free worker BEFORE deployment. Build the exact copied SDR application sources and original Dockerfile (its historical VRT label is not provenance). Record actual image ID and source hashes. Deploy only original four-radio-sdr topology with reviewed companion files; effects include management network172.31.79.0/24, seven links, application eth1 IP/MTU setup, SRL MAC-VRF/mirroring startup configuration and SDR processes. No diagnostic capture, application monitoring or health inference is authorized. Recorder's existing topology command is retained, not invoked as an extra capture job.

Qualification: exact native graph associations; complete companion inventory/hash admission and missing-file refusal; all14 observed aliases/interfaces when available;20 native-backed refreshes with unchanged6s/256KiB guest,9s transport,12s browser,1-collection budgets. Source/declaration/runtime stages separate. Real browser on-demand and selected runtime refresh. Existing offline tests plus new deployment-policy tests for wrong lab, mismatched names/full IDs, old-session incompatibility, malicious fields and missing files. Identity mutation tests on synthetic cloned responses, clearly classified; no extra lab deployment or destructive topology editing required. Exercise actual cancellation/refresh recovery. Native runtime failures preserve declaration usefulness. Failures cannot be repaired by substituting images/apps or editing source YAML.

Scoped cleanup: restore any exact trial-only fault injection, destroy /opt/four-radio/generated/four-radio.clab.yml; verify exact task container labels/network absent; stop only newly created VM. No pre-existing VM access. Preserve failed attempts. No repeated30-minute soak because poll/transport lifetime code is unchanged.
