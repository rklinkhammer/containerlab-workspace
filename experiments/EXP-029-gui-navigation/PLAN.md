# Independent acceptance expectations, before implementation

Verified A24 all1472 manifest files; clean Git baseline. Phase1 reconciles evidence, Phase2 improves existing approved-bundle GUI navigation (not arbitrary uploads/source ingestion).

1. Recorded and newly executed source provenance stay explicit; native response source/bundle/job identity must match selected approved input.
2. Disconnect cancels configuration/observation requests, clears observations and log bindings, stops auto refresh, and retains declaration selection. Late replies cannot reconnect or restore discarded evidence.
3. Reconnect starts without previous observations/logs. Config errors are bounded/sanitized. Concurrent connects cannot win out of order.
4. Snapshot replacement/identity conflicts invalidate binding and selected object; no old runtime values or log controls remain enabled. Transient failure retains only labeled last-known evidence, and explicit refresh can recover.
5. Repeated or older timestamps cannot promote last-known data to fresh success.
6. Source switching clears selection/runtime. Parallel links, disconnected/special/unsupported objects remain represented using existing independent model fixtures. Test actual recorded four-radio plus a differently shaped fixture, no application fixture-name branches.
7. Keep approved layout; keyboard/table/canvas inspector selection and existing logs/capture placeholders remain usable. No console/capture/lifecycle capability added. Build, unit and targeted browser evidence separate from unrun live VM checks.
